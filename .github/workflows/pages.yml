name: Build and publish mod index

on:
  push:
    branches: [main]
  schedule:
    # Check the ZIP repository every hour. Scheduled workflows can start a few
    # minutes late when GitHub Actions is busy.
    - cron: '17 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: mod-index
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Synchronize FAFF0x Gen 2 ZIP mods
        env:
          GITHUB_TOKEN: ${{ github.token }}
          MOD_SOURCE_REPO: FAFF0x/gen2recomp
          MOD_SOURCE_REF: main
          MOD_INDEX_AUTHOR: FAFF0x
          FORCE_SYNC: ${{ github.event_name != 'schedule' }}
        run: node scripts/sync-personal-mods.mjs

      - name: Commit synchronized entries
        run: |
          changes=$(git status --porcelain -- mods .github/faff0x-source-state.json)
          if [ -z "$changes" ]; then
            echo "No metadata changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mods .github/faff0x-source-state.json
          git commit -m "Sync FAFF0x mod ZIPs [skip ci]"
          git push

      - name: Build index
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node scripts/build-index.mjs --releases

      - name: Commit generated index
        run: |
          changes=$(git status --porcelain -- site/data/index.json site/data/mod.schema.json)
          if [ -z "$changes" ]; then
            echo "No generated index changes to commit."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add site/data/index.json site/data/mod.schema.json
            git commit -m "Update generated mod index [skip ci]"
            git push
          fi

      - name: Summarize
        run: |
          {
            echo '### index.json'
            echo
            node -e "const i=require('./site/data/index.json');
              console.log('- ' + i.count + ' mod(s), generated ' + i.generated_at);
              for (const m of i.mods) console.log('- \`' + m.folder + '\` ' + (m.latest ? m.latest.version + ' (' + m.update_check + ')' : m.version + ' (' + m.update_check + ')'));"
          } >> "$GITHUB_STEP_SUMMARY"

